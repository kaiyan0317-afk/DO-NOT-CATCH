workflows:
  unity-android-build:
    name: Unity Android Build
    instance_type: mac_mini_m2
    max_build_duration: 60
    scripts:
      - name: Install Unity
        script: |
          # Codemagic公式のインストーラーを使ってインストール
          unity-installer install --version 2022.3.62f3 --components android
      - name: Build Android
        script: |
          # インストールされたパスにあるUnityを実行
          # (unity-installerで入れた場合、このパスに配置されます)
          /Applications/Unity/Hub/Editor/2022.3.62f3/Unity.app/Contents/MacOS/Unity \
            -batchmode \
            -quit \
            -projectPath DWeb/FISHTARGET \
            -executeMethod BuildScript.BuildAndroid \
            -logFile /dev/stdout
    artifacts:
      - DWeb/FISHTARGET/build/android/*.apk
