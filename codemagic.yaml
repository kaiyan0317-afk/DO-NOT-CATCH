workflows:
  unity-android-build:
    name: Unity Android Build
    instance_type: mac_mini_m1
    max_build_duration: 60
    scripts:
      - name: Build Android
        script: | 
          echo "Building..."
    artifacts:
      - build/android/*.apk
